# Public Key Placeholders

The private repo keeps operator and user SSH public keys under this directory.

The public mirror omits those keys on purpose. Add example files here only when
you want to document the expected format.

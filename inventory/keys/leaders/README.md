# Leader Deployer Keys

This directory is intentionally empty in the public mirror.

In the private repo it contains trusted leader-machine deployer public keys that
are merged into root SSH access for managed hosts.

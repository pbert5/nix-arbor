# Complex Inventory

This subfolder preserves the more involved public inventory examples without
making them the default entry point.

It shows how the same inventory model can expand to include:

- annex storage
- SeaweedFS hot storage roles
- Radicle seed nodes
- tape-backed archive nodes

The main `inventory/` files stay intentionally smaller. Copy from this folder
when you want to grow into the more advanced patterns.

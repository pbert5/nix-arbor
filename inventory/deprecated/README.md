# Deprecated Inventory

This directory keeps retired inventory records close to the active inventory
without keeping them in the evaluated cluster graph.

Use it for hosts, bootstrap targets, key recipient records, and network nodes
that are useful as reference material but should not be exported, validated, or
published as live cluster state.

[project]
name = "clusterctl"
version = "0.1.0"
description = "Live signed cluster identity registry tooling"
requires-python = ">=3.11"

[project.scripts]
clusterctl = "clusterctl.main:main"
clusterchk = "clusterctl.main:main"
clusterplan = "clusterctl.main:main"
